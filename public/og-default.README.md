# OpenGraph image (drop-here)

`SEOHead.astro` references `/og-default.png` (1200×630). Drop the real PNG here
as `og-default.png`.

Until then, social previews will 404 the image — everything else works. For
per-project OG images, set `ogImage` in a project's MDX frontmatter (the slot is
already wired through `SEOHead`).
